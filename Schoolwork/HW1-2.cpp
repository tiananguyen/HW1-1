#include <iostream>

/*Assume that isIsosceles is a bool variable, and that the variables isoCount, triangleCount, and
polygonCount have all been declared and initialized. Write a statement that adds 1 to each of
these count variables (isoCount, triangleCount, and polygonCount) if isIsosceles is true.*/

int main() {
  int isoCount;
  int triangleCount;
  int polygonCount;

  if(isIsosceles==1) {
	   isoCount++;
	   triangleCount++;
	   polygonCount++;
  }
}
